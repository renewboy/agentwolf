# Implemented Agent Notes

See [the parent AGENTS.md](../AGENTS.md). These Notes describe shipped decisions.

Keep realization facts current without appending change history. A reversal requires a new,
cross-linked Note. When a decision no longer guides future work, move it to the frozen archive rather
than continuing to maintain stale implementation detail.
