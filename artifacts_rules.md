# 当前态交付物纯净性规则

本文只定义与具体项目、产品和代码库无关的通用交付物纯净性规则。
任何项目特定的架构、实现、组件、工具、资产路径、测试或运行时要求，都必须写入该项目的相关模块文档，
不得通过新增、修改或扩展本文件来承载。

## 1. 上下文与交付物严格分离

用户提供的上下文可能同时包含当前方案、旧设计、讨论过程、失败尝试、临时实现、纠正意见和未来设想。

这些内容都可以用于理解和判断，但不能直接等同于最终交付内容。

生成正式交付物前，应先完成内部归一化：

1. 识别当前有效状态；
2. 排除已失效、被替换或仅存在于讨论过程中的内容；
3. 区分当前事实、历史背景和未来设想；
4. 只保留与本次交付目标直接相关的信息。

例如，用户为了说明当前方案而提到“最初计划用 A，后来发现不支持，因此采用 B”，如果当前正式方案是 B，那么当前架构文档中应直接描述 B，而不是保留这段演进过程。

上下文是推理材料，不等于交付内容。

---

## 2. 默认采用当前态视角

除非用户明确要求历史分析、版本对比、迁移指南、变更日志、兼容性说明、复盘或路线图，否则正式交付物默认只描述当前有效方案。

应重点描述：

- 当前系统有什么；
- 各部分负责什么；
- 当前如何运行；
- 输入、输出和接口是什么；
- 当前有哪些约束和边界。

不要主动解释当前设计是从什么方案演变而来，也不要为了“交代完整”把新旧方案同时写进去。

例如，组件名称应写成 `Template Planner`，而不是“新版 Template Planner”或“替代旧 Pipeline 的 Planner”。

---

## 3. 禁止迁移叙事和辩解性表达

当前态交付物应直接陈述当前事实，不通过历史反面来定义当前设计。

避免使用带有迁移、替代、修正或回应争议意味的表达，例如：

- “原来……现在改为……”
- “相比上一版……”
- “替代旧方案……”
- “不再使用……”
- “为了避免之前的问题……”
- “这里不是……而是……”

如果某个历史事实不会影响目标读者当前的理解或操作，就不应出现在交付物中。

例如，与其写：

> Android 渲染模块，不再提供 macOS dylib 兜底。

应直接写：

> Android NLE Render Host 负责执行视频渲染。

但如果否定本身就是当前系统的重要约束，则可以保留，例如“不得访问授权范围之外的数据”。

---

## 4. 信息必须匹配目标受众

任何内容在进入交付物前，都应判断它是否对目标读者有实际价值。

核心标准是：

- 读者能否理解；
- 能否据此采取行动；
- 是否需要该信息才能正确理解或执行任务。

不同交付物的信息边界应不同。

面向产品或普通用户时，重点描述产品行为、使用方式和可观察结果。

面向技术读者时，可以描述系统职责、接口、数据流、部署和运行边界。

面向外部模型时，只应提供模型完成任务所需的目标、输入、输出、约束和必要定义。

面向内部实现者时，可以保留更多实现细节，但这些细节仍必须属于当前有效方案。

不要因为某个信息存在于项目背景中，就默认目标读者需要知道它。

---

## 5. 内部术语和实现细节必须经过必要性审查

内部名称、缩写、代号、路径、类名、函数名、协议名和工程术语，只在它们确实属于交付物的一部分时保留。

通常只有以下情况值得保留：

- 它是正式接口或数据格式中的真实名称；
- 是目标读者需要操作或调用的对象；
- 删除后会造成理解或执行错误；
- 已经在文档中得到清晰定义。

否则应优先替换为职责或领域含义。

例如，对外部模型描述任务时，与其要求它“调用内部的 `RequestCompilerV2`”，不如描述为“将规划结果转换为渲染请求”，除非 `RequestCompilerV2` 本身就是模型必须输出或操作的正式对象。

同样，与交付目标无关的目录结构、调试工具、临时缓存、迁移脚本和问题排查细节不应进入正式交付物。

---

## 6. 外部模型提示词必须独立成立

提供给外部模型的提示词必须被视为一份独立的执行契约。

默认假设模型不知道项目历史，也无法访问任何未明确提供的内部信息。

因此提示词中应包含：

- 任务目标；
- 输入以及输入字段的含义；
- 模型需要执行的行为；
- 输出内容和格式；
- 质量标准；
- 必要约束；
- 必须理解的概念定义。

不要把开发过程中的讨论直接塞进提示词。

例如，不应告诉模型：

> 之前模型经常输出旧字段，所以这次不要再这样做。

应直接定义：

> 输出只能包含 schema 中定义的字段。

外部模型需要知道“应该怎么做”，而不是“这条规则为什么产生”。

---

## 7. 架构图和流程图只表现当前系统

架构图、流程图、时序图应像当前系统的地图，只展示当前运行真正存在的内容。

可以包含：

- 当前组件与职责；
- 当前接口；
- 当前数据流和控制流；
- 当前存储和运行边界；
- 当前依赖关系；
- 必要的异常和恢复机制。

不要把历史设计、废弃组件、迁移路径、候选方案、临时实现或未来设想画进当前架构。

组件命名也应直接描述职责，不应包含“新版”“旧版”“替代”“兼容”“兜底”等历史关系。

例如，应使用：

> Cloud-safe Context

而不是：

> 不再上传原图的 Cloud Context

前者描述当前对象，后者描述的是它与某个历史方案之间的关系。

---

## 8. 当前、历史和未来必须隔离

项目中的信息应明确分为三类：

**当前事实**：已经确认、已经有效或属于当前正式设计的内容。

**历史信息**：旧方案、迁移过程、废弃设计、决策原因和问题复盘。

**未来信息**：规划、设想、候选方案和尚未落地的能力。

默认正式交付物只包含当前事实。

如果用户确实需要其他内容，应分别放入对应的文档或明确章节中，例如：

- 历史与设计决策；
- 迁移说明；
- 路线图；
- 待实现能力。

不要把“以后可能支持”的能力直接画进当前架构，也不要把历史遗留行为描述成当前系统必须承担的约束。

---

## 9. 保持交付物类型纯粹

不同文档应服务于不同目的。

例如：

- 当前架构回答“系统现在如何组成和运行”；
- 接口规范回答“如何调用以及数据格式是什么”；
- 使用说明回答“用户如何操作”；
- 迁移指南回答“如何从一个状态迁移到另一个状态”；
- 变更日志记录“发生了什么变化”；
- 决策记录解释“为什么选择某种设计”；
- 路线图描述“未来准备做什么”。

不要因为这些信息都与同一个项目有关，就把它们混入同一份交付物。

当用户要求当前架构、当前提示词或当前技术方案时，只输出完成这个目标所需的内容，不主动附带历史、复盘、迁移或规划信息。

---

## 10. 输出前执行静默清理

正式提交前，应对内容做一次静默检查。

重点确认：

- 是否描述的是当前有效状态；
- 是否混入了历史或未来内容；
- 是否存在迁移、比较或辩解性表达；
- 信息是否对目标读者真正有用；
- 是否包含未定义或不必要的内部术语；
- 是否泄露了与交付目标无关的实现细节；
- 是否可以在不了解项目历史的情况下独立理解；
- 对于外部模型提示词，是否仅凭当前输入就可以执行。

如果删除一句话后不会影响目标读者正确理解或执行任务，通常应删除。

如果当前状态存在冲突，应以最近且明确确认的信息为准。无法可靠判断时，不得自动继承旧方案、拼接多个版本或把猜测写成当前事实。

最终输出只呈现纯净的当前态交付物，不解释删掉了什么，也不说明执行了哪些清理步骤。
