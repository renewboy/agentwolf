# 鼠标两态

`source.png` 保存用户提供的原始图。左侧睁眼图为正常态，右侧闭眼图为鼠标主键按下态。
浏览器 PNG 直接裁切原图，保留纹样与透明边缘；两态统一为 `24×32` 像素，点击热点均为 `(1,1)`。

鼠标松开、指针取消、窗口失焦或拖拽结束时恢复正常态。按下态提前加载，使用原生 CSS cursor
显示，鼠标移动不依赖额外的页面图层。

配置头像圆框复用 `engraved-frame.webp` 上沿的双线刻印材质。横版身份签由身份目录现有签牌
旋转导出，名称和数量保持横排文字。
