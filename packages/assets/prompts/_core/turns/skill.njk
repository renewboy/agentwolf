{% include "_core/turns/context.njk" %}
请通过 `trigger_skill` 工具处理当前技能。`abilityId` 只能是 {% for abilityId in phase.allowedAbilityIds %}`{{ abilityId }}`{% if not loop.last %} / {% endif %}{% endfor %}；发动技能时提交合法目标，放弃时提交空目标和 `option: pass`。工具返回接受回执后结束本回合。
