{% if actionToolNames.length == 1 %}请调用 `{{ actionToolNames[0] }}` 提交本轮唯一一次行动。{% else %}请调用 `{{ actionToolNames[0] }}` 发动技能，或调用 `{{ actionToolNames[1] }}` 明确放弃，只提交本轮唯一一次行动。{% endif %}工具返回接受回执后立即结束本回合，不要输出发言。
