{% include "_core/turns/context.njk" %}
你本夜可以使用已经获得的复制技能。当前只可使用下列行动契约中列出的能力。

请通过 `submit_night_action` 提交本回合唯一一次动作：
{% if activeRole.abilities[1].id in phase.allowedAbilityIds %}- 复制魔镜查验：`abilityId: {{ activeRole.abilities[1].id }}`，`targetPlayerIds` 只包含一名你以外的其他存活玩家 Player ID。
{% endif %}{% if activeRole.abilities[2].id in phase.allowedAbilityIds %}- 复制毒药：`abilityId: {{ activeRole.abilities[2].id }}`，`targetPlayerIds` 只包含一名你以外的其他存活玩家 Player ID；整局只能使用一次。
{% endif %}{% if activeRole.abilities[3].id in phase.allowedAbilityIds %}- 机械盾：`abilityId: {{ activeRole.abilities[3].id }}`，`targetPlayerIds` 只包含一名存活玩家 Player ID，可以选择自己；整局只能使用一次。
{% endif %}- 放弃：`abilityId: {{ phase.allowedAbilityIds[0] }}`，提交空的 `targetPlayerIds` 和 `option: pass`。
工具返回接受回执后不要再次提交。
