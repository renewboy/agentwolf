{% if actor.playerId == payload.data.loverIds[0] %}你与{{ helpers.player(payload.data.loverIds[1]) }}成为情侣。你不知道对方的具体身份，也不知道未自连时丘比特是谁。你不能在白天放逐或放逐 PK 中投给对方；任一方出局时，另一方立即殉情。{% elif actor.playerId == payload.data.loverIds[1] %}你与{{ helpers.player(payload.data.loverIds[0]) }}成为情侣。你不知道对方的具体身份，也不知道未自连时丘比特是谁。你不能在白天放逐或放逐 PK 中投给对方；任一方出局时，另一方立即殉情。{% else %}你连接的情侣是{{ helpers.playerList(payload.data.loverIds) }}。你不知道他们的具体身份；你的胜负跟随情侣构成。{% endif %}
